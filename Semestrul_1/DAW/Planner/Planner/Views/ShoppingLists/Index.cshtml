@model IEnumerable<Planner.Models.ShoppingList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titlu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titlu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShoppingListId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShoppingListId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShoppingListId })
        </td>
    </tr>
}

</table>
